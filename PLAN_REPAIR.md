# FRANKEN — План починки и приведения к «code-only mirror»

## Цель
- GitHub = зеркало **кода/конфигов**; данные/результаты/логи/venv/аудиты — **локально**.
- Фабрика должна запускаться предсказуемо: сборка окружения, smoke-компиляция, затем запуск.

## P0 (сразу, чтобы фабрика стартовала)
1) Удалить из репозитория мусор: _FULL_XRAY_AUDIT_*, AUDIT_*, временные папки, архивы.
2) Закрепить .gitignore (code-only mirror).
3) Пересобрать .venv из equirements.frankenstein.txt.
4) Smoke-check: py_compile ключевых модулей + быстрый -h для CLI.

## P1 (стабилизация модулей)
- Привести импорты к одному каноническому пути (например, ga_adapters/runner_bridge.py + thin-wrapper в корне).
- Вынести все зависимости/вендоры в endor/ и фиксировать версии.
- Добавить 	ools\\doctor.ps1: автодиагностика окружения/путей/данных/параметров запуска.

## P2 (производительность и чистота)
- Кэш/результаты/датасеты только локально (папки-скелеты остаются).
- Единый формат данных (parquet), единые метрики, единый формат отчётов.
