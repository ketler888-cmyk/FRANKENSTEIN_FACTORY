# ================= FRANKEN | CODE-ONLY MIRROR (HARD IGNORE) =================

# python env
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd

# data (local only)
SCALPING_DATA/
SCALPING_DATA_PARQUET/
SCALPING_DATA_RAW/
*.parquet
*.csv
*.tsv
*.feather
*.arrow

# databases / state (local only)
*.duckdb
*.db
*.sqlite
*.sqlite3

# results / cache / logs (local only)
results/
results_ga/
cache/
logs/
*.log
*.out
*.err

# backups / audits (local only)
BACKUPS_MIRROR/
_BACKUPS_MIRROR/
AUDIT_*/
_AUDIT_*/
АУДИТ_*/
ЗЕРКАЛЬНЫЕ_ОТЧЕТЫ/
КОНТЕКСТНЫЙ_ЧАТ*/
ЖЕЛЕЗНЫЙ_ЗОНД*/
*.zip
*.7z
*.rar

# runtime artifacts (local only)
ops/heartbeat.json
ops/*.pid
ops/STOP_FACTORY
ops/vendor.lock.json

# os trash
Thumbs.db
desktop.ini
.DS_Store
